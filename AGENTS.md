# MADSearcher development

Read `Docs/技术方案.md`, `Docs/设计文档.md`, and `Docs/tasks.json` before continuing development. Preserve the user's original `Docs/需求文档.md`.

Only start a DAG node after all dependencies have status `completed`. Update state using `python scripts/dag.py --task <id> --status <state> --evidence <reason>`. Do not mark real model or Adobe validation passed without running it.

User direction on 2026-09-10: finish code, build/package, and write usage documentation, but do not run new or repeat tests until the user supplies further test instructions. Historical test reports remain evidence only for the code and fixtures they actually exercised. The `acceptance` DAG node is deferred; do not treat it as passed or start it autonomously under this instruction.

The WPF frontend and Python worker communicate through UTF-8 NDJSON. stdout is protocol only. The user now requests plaintext API key persistence in the ignored local settings file; CLI may read that setting, with an environment variable override. Never log keys or put them in command arguments or version control. Original video files are read-only; all generated data stays in the repository by default.

Local feedback lives in `Docs/优化及bug修复.md`: do not track or upload this file. Mark each implemented item 已完成, and leave 待定 items unimplemented. Active tasks use captured settings and form inputs; browsing and editing should remain available while only new task actions are disabled. Do not add a task queue.

Build: `dotnet build src/MADSearcher.Desktop/MADSearcher.Desktop.csproj -c Release`. Test: `.venv/Scripts/python.exe -m unittest discover -s tests -v`. Reproducible project issues belong in `.agents/skills/` with narrow triggers and observed fixes.

Release baseline: user explicitly finalized v0.1.0 at commit `35edf258645c9f6cd091a261e60c59639b929cae`. Never move or replace that tag again. Later development stays on dev. Feedback item 4 was authorized and implemented. The user now authorizes item 7: separate collapsible source/result previews, frame timeline and work range, on-demand buffering and continuous prefetch with initial loading allowed. Preview decoding may run independently of the single processing task; it must be bounded, cancellable and must not alter captured task inputs. Feedback item 8 and functional acceptance remain deferred.

User correction on 2026-09-13: the 512-pixel upload cap applies only to indexing images. Cutout localization uploads its first frame and optional reference at original pixel dimensions; keep this policy separate from index preprocessing, SAM source pixels and the reduced-resolution UI preview. This does not authorize switching to image generation. Functional tests remain deferred.

User approved feedback item 11 on 2026-09-13: local shot detection, dynamic index sampling, layered caches and shot-aware results. PySceneDetect is pinned to 0.6.6 to retain the frame-index API and compatibility with existing headless OpenCV/Click dependencies. Database version 2 backs up before adding shot metadata. Feedback 12/13 and functional tests remain deferred.

User subsequently authorized feedback 13 on 2026-09-13: embedded character names/aliases, local query planning, event-first fusion and bounded query-vector caching using existing indexes. Feedback 12 (visual identity accuracy) and functional tests remain deferred. Query-only changes must not invalidate visual indexes or change their prompts. Ranking scores are engineering weights, not identity probabilities; ambiguous names and neighbouring evidence must not become hard identity claims.

User approved the first three dialogue-search improvements: independent subtitle cue/context vectors, separate lexical/semantic retrieval, and combined/dialogue/scene modes. Add an explicit subtitle-only backfill action that never reruns vision, media decoding or ASR. Preserve existing visual indexes and caches; back up before database version 3 migration. Do not execute paid backfills or migrations as tests. Reranking models, feedback 12 and functional acceptance remain deferred.

User authorized ASS cleanup after a 44,271-event subtitle hit the cue limit: filter drawing spans in tag order, conservatively merge repeated rendering events, and apply the cue limit to cleaned records. Keep source subtitles read-only. ASS cleanup has its own subtitle manifest version; do not invalidate SRT/VTT, ASR or all visual indexes. Report cleanup counts without subtitle text. Functional tests and real-file backfills remain deferred.

User approved feedback 14 on 2026-09-14: AE export supports rgba, matte (default: original frames plus editable Alpha repair precomp), and optional paths. Preserve existing RGB/alpha pixels; do not simplify, shrink or drop components to force AE export. New cutout.export_ae must use completed task manifests and existing PNGs without rerunning SAM, media decoding or cloud calls. Keep old manifests/scripts and source frames; write a separate export bundle. Pixel scripts remain available when optional vector export fails. Real AE and other functional tests remain deferred.

User approved feedback 15–17 on 2026-09-14: add Tools cache cleanup and open-index-directory entries; separate durable thumbnails from disposable samples. Preserve the library database, referenced assets and all task results including incomplete cutouts. Scan before deleting, migrate old cache-backed thumbnail references transactionally before cleanup, use explicit owned roots and reject links/reparse traversal. Public worker commands share an activity lock; cleanup is exclusive. Do not run actual cleanup, data migration or functional tests during implementation under the existing test pause.

User authorized simpler AE editing on 2026-09-15: keep one outward-facing result comp, explicit restore/erase and whole-edge controls. Restore uses the synchronized opaque source frames for color preview; edge effects process the combined Alpha before it mattes the untouched original color. Default controls must leave the original boundary unchanged. Preserve existing JSX and user-edited AE comps; only new exports receive script_version=2. Do not execute AE scripts, reexport user results or run functional tests under the existing pause.
